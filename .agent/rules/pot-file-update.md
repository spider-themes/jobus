---
trigger: always_on
---

Whenever new translatable text is added to the codebase, ensure that the corresponding .pot language files are updated accordingly to keep all localization resources in sync.
